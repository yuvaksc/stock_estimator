# ── Real-Time Stock-Sentiment AI System — backend dependencies ───────────────
# Install:  pip install -r requirements.txt
# (torch is pinned loosely so an existing CUDA build, e.g. 2.5.1+cu121, satisfies it.)

# Web / API
fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
websockets>=12.0
httpx>=0.27

# Database (async)
SQLAlchemy[asyncio]>=2.0
asyncpg>=0.29
greenlet>=3.0

# Cache / Redis (async client lives in redis>=5)
redis>=5.0

# Streaming (Kafka API — works against Redpanda)
aiokafka>=0.10

# LLM sentiment
groq>=0.9
instructor>=1.3

# Local models (FinBERT + MiniLM embeddings)
# transformers>=4.52 provides transformers.modeling_layers, required by
# sentence-transformers>=4 (older builds raise ImportError at model load).
transformers>=4.52,<5
sentence-transformers>=3.0
torch>=2.0
numpy
pandas
scikit-learn

# Data sources
asyncpraw>=7.7

# MLOps (optional but imported by tracking.py / train_regressor.py)
mlflow>=2.12
# wandb>=0.17            # optional alternative tracker

# ── dev / test (install with: pip install -r requirements.txt) ───────────────
pytest>=8.0
pytest-asyncio>=0.23
fakeredis>=2.23
ruff>=0.4
